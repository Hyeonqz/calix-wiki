# TIL - 2026-05-22 (목)

## 오늘 한 일

| # | 작업 내용 |
|---|----------|
| 1 | FE — 개발자 전용 메뉴 추가 (`layout/AppMenu.tsx`): Keycloak `develop` 역할 보유자만 '개발자' 메뉴 섹션 노출 |
| 2 | FE — 국내 거래내역 조회 초기화 버튼 버그 수정 (`SearchBar.jsx`): `reset(defaultValues)` 호출 시 `defaultValues` 변수 미정의 문제 해결 |
| 3 | Git — 루트 `.gitignore` 추가: `.omc/`, `.omx/`, `.idea/`, `tsconfig.tsbuildinfo` 추적 제외 |

## 배운 것

| # | 내용 |
|---|------|
| 1 | **react-hook-form `reset()` 동작** — `reset(undefined)`는 아무 동작 안 함. 인자 없이 `reset()` 호출 시 `useForm` 초기화 시점의 `defaultValues`로 리셋. 동적 값이 필요하면 `reset(getDefaultValues())` 형태로 호출 |
| 2 | **AppMenu 역할 기반 필터링 전략** — 기존 `filteredModel`의 복잡한 역할별 분기를 건드리지 않고, `finalModel`로 한 번 더 필터링하는 방식이 안전 |

## 이슈 / 트러블슈팅

| # | 이슈 | 해결 |
|---|------|------|
| 1 | `git rm --cached` 후 `git reset` 주의 — 캐시 제거 후 실수로 `git reset HEAD -- .`를 하면 제거가 되돌려짐 | 캐시 제거와 스테이징은 한 흐름에서 처리 |

## 내일 할 일

| # | 계획 |
|---|------|
| 1 | 개발자 전용 메뉴에 하위 항목 추가 |
