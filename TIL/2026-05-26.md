# TIL - 2026-05-26 (월)

## 오늘 한 일

| # | 작업 내용 |
|---|----------|
| 1 | 간편결제사 목록 정렬 기준 변경 — `pc.code.asc()` → `pc.paymentCompanyId.asc()` (BE QueryDSL) |
| 2 | 간편결제사 Error 코드 조회 API 구현 (BE) — Controller, UseCase, Service, Port, Adapter, DTO 전체 |
| 3 | 간편결제사 상세 Dialog TabView/TabPanel 개편 (FE) — URL 목록 탭 + Error 코드 목록 탭, 탭별 lazy 로딩 |
| 4 | FE 타입 정의(`PaymentCompanyError`), API 엔드포인트, Mapper 함수 추가 |
| 5 | Wrapper.tsx 간소화 — detail API 프리로딩 제거, 탭에서 lazy 호출로 전환 |
| 6 | 간편결제사 Error 코드 등록/수정 API 구현 (BE) — Input DTO(Register/Update record), Service CRU 로직, Controller POST 엔드포인트 2개 |
| 7 | 간편결제사 Error 코드 등록/수정 화면 구현 (FE) — Error 등록/수정 Dialog, API 엔드포인트(errors.register/update), Mapper(toBEErrorRegister/Update) |
| 8 | Error 코드 목록 조회 페이징 처리 (BE+FE) — QueryDSL offset/limit + PageImpl, FE DataTable lazy paginator |
| 9 | 간편결제사 상세 Dialog 저장 버튼 위치 변경 — footer → 폼과 TabView 사이(오른쪽 정렬) |
| 10 | `docs/03_Features/payment-company-error-code.md` 구현 문서 업데이트 — CRU 전체 변경사항, API 명세, 테스트 시나리오 12건 |

## 배운 것

| # | 내용 |
|---|------|
| 1 | PrimeReact `TabView`의 `onTabChange` 이벤트로 lazy 로딩 구현 시, `loaded` 플래그를 사용해 중복 API 호출을 방지하는 패턴이 효과적 |
| 2 | JPA `@EmbeddedId` 복합키(PaymentCompanyErrorId) 엔티티를 QueryDSL로 조회할 때 `pcError.id.paymentCompanyCode.eq(code)` 형태로 접근 |
| 3 | Detail Dialog에서 탭별 lazy 로딩 전환 시, Props 타입을 `PaymentCompanyDetail` → `PaymentCompany`로 변경하면 Wrapper 쪽 detail API 호출을 완전히 제거 가능 |
| 4 | Spring `Page.map()`으로 엔티티→DTO 변환하면 페이징 메타(totalElements, number 등)가 자동 유지됨 — stream().map().toList() 대비 간결 |
| 5 | PrimeReact DataTable `lazy` + `paginator` 조합 시 `first`, `rows`, `totalRecords`, `onPage` 4가지가 필수. 부모 Dialog 내 중첩 DataTable에서도 동일 패턴 적용 가능 |

## 이슈 / 트러블슈팅

| # | 이슈 | 해결 |
|---|------|------|
| 1 | `error.message` TS 컴파일 에러 — `ApiService.call` 반환 error 객체에 `message` 속성이 타입 미정의 | 기존 프로젝트 패턴 확인 후 하드코딩 문자열로 통일 (`'등록에 실패했습니다.'`) |
| 2 | Edit 도구 사용 시 "File has not been read yet" 에러 | 파일을 Read로 먼저 읽은 후 Edit 적용으로 해결 |

## 내일 할 일

| # | 계획 |
|---|------|
| 1 | 간편결제사 Error 코드 CRU + 페이징 동작 통합 테스트 |
| 2 | 에러 페이지 개선 (404/500 커스텀 페이지) 구현 계획 수립 |
| 3 | Utrack 이슈 등록 (FEAT-payment-company-management, FEAT-error-page-improvement) |
