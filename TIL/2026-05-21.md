# TIL - 2026-05-21 (수)

## 오늘 한 일

| # | 작업 내용 |
|---|----------|
| 1 | Gradle Spotless 플러그인 도입 — 전체 멀티모듈 프로젝트에 Naver 코딩 컨벤션 자동 포맷팅 적용 |
| 2 | IntelliJ HTTP Client `.http` 파일 pre-request script 수정 (Node.js → IntelliJ 내장 API 전환) |
| 3 | Kafka 서버 설정 통합 — `qrbank.kafka.dqms.servers` + `qrbank.kafka.oqms.servers` → `qrbank.kafka.servers`로 단일화 (OQMS 서버 값 사용, Java 7개 파일 + kafka.yml 수정) |

## 배운 것

| # | 내용 |
|---|------|
| 1 | **Spotless + Naver formatter 적용 방법** — IntelliJ formatter XML은 Spotless에서 지원하지 않음. Eclipse formatter XML만 사용 가능. IDE는 IntelliJ formatter, CI/빌드는 Eclipse formatter로 역할 분리 |
| 2 | **Gradle 멀티모듈 플러그인 패턴** — 루트에서 `apply false`로 버전만 선언하고, `subprojects` 블록에서 실제 적용하는 것이 표준 패턴 |
| 3 | **AI 에이전트 포맷팅 전략** — 에이전트 지시(`CLAUDE.md`, `CODEX.md`)보다 Spotless 같은 도구 강제가 신뢰도 높음 |
| 4 | **Kafka 멀티클러스터 설정 통합** — DQMS/OQMS 클러스터를 별도 프로퍼티로 관리하던 구조에서 단일 `qrbank.kafka.servers`로 통합. Bean 설정이 다르면 Bean 자체는 유지하는 것이 안전 |
| 5 | **IntelliJ HTTP Client JavaScript 런타임** — Node.js가 아님. `require()` 사용 불가. 내장 `crypto` API와 `Date.now().toString(36)` 조합으로 대체 |

## 이슈 / 트러블슈팅

| # | 이슈 | 해결 |
|---|------|------|
| 1 | WSL I/O 에러 — `./gradlew spotlessApply` 실행 시 `ChecksumService` I/O 에러. WSL에서 Windows 파일시스템(`/mnt/c/`) 접근 시 간헐적 발생 | Windows PowerShell에서 `.\gradlew.bat spotlessApply` 실행으로 우회 |
| 2 | HTTP Client `require is not defined` — IntelliJ HTTP Client는 자체 JS 런타임 사용 | Node.js `require('crypto')` → IntelliJ 내장 `crypto` 전역 객체로 교체 |

## 내일 할 일

| # | 계획 |
|---|------|
| 1 | Windows PowerShell에서 `gradlew.bat spotlessApply` 실행하여 전체 프로젝트 포맷 적용 확인 |
| 2 | `CLAUDE.md`, `CODEX.md`에 `spotlessApply` 실행 지시 추가 |
| 3 | Kafka 설정 통합 후 폐쇄망 배포 — 기동 시 모든 Producer/Consumer가 통합 서버로 연결되는지 로그 확인 |
